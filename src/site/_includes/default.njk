<!DOCTYPE html>
<html lang="de">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>
            {% if title %}
                {{ metadata.title }} &mdash; {{ title }}
            {% else %}
                {{ metadata.title }}
            {% endif %}
        </title>
        <meta name="description" content="{{ description if description else metadata.description }}">
        <link rel="stylesheet" href="/styles/index.css">
    </head>
    <body>
        <div id="layout">
            <header id="header">
                <{{ "h1" if page.url == "/" else "span" }}  id="logo">
                    <a href="/" >
                        {{ metadata.title }}
                    </a>
                </{{ "h1" if page.url == "/" else "span" }}>
                <span id="explanation">
                </span>
            </header>
            <main>
                {{ content | safe }}
            </main>
            <footer>
                <ul id="social-links">
                    <li>
                        <a rel="me noreferrer" href="{{ metadata.social.mastodon }}">
                            <img src="/img/icons/mastodon.svg" alt="Mastodon">
                        </a>
                    </li>
                    <li>
                        <a  rel="me noreferrer" href="{{ metadata.social.twitter }}">
                            <img src="/img/icons/twitter.svg" alt="Twitter">
                        </a>
                    </li>
                </ul>
            </footer>
        </div>
        <script src="/scripts/main.js" defer></script>
    </body>
</html>
